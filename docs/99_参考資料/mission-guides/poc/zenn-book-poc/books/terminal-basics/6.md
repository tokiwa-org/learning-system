---
title: "理解度チェック"
free: true
---

# 理解度チェック：黒い画面の正体を知ろう

Step 1で学んだ内容の理解度をチェックします。

- 全10問
- 合格ライン: 80%（8問正解）
- 不合格の場合は復習してから再挑戦してください

## 問題

### Q1. ターミナルの役割として正しいものはどれですか？

- A) コマンドを解釈して実行する
- B) 文字を表示し、キーボード入力を受け付ける
- C) ファイルを保存する
- D) インターネットに接続する

:::details 答えを見る
**正解: B**

ターミナルは「文字を表示するウィンドウ」です。
コマンドを解釈・実行するのはシェルの役割です。
:::

---

### Q2. シェルの役割として正しいものはどれですか？

- A) 黒い画面を表示する
- B) 入力されたコマンドを解釈して実行する
- C) ファイルを削除する
- D) プログラムを作成する

:::details 答えを見る
**正解: B**

シェルは「通訳者」のような役割で、ユーザーが入力したコマンドを解釈し、OSに伝えます。
:::

---

### Q3. CLIとGUIの説明として正しいものはどれですか？

- A) CLIはマウス操作、GUIはキーボード操作
- B) CLIはキーボード操作、GUIはマウス操作
- C) CLIとGUIは同じもの
- D) CLIはMac専用、GUIはWindows専用

:::details 答えを見る
**正解: B**

- CLI（Command Line Interface）= キーボードで文字入力
- GUI（Graphical User Interface）= マウスでクリック操作
:::

---

### Q4. プロンプトに表示される「~」は何を意味しますか？

- A) ルートディレクトリ
- B) ホームディレクトリ
- C) デスクトップ
- D) ダウンロードフォルダ

:::details 答えを見る
**正解: B**

`~`（チルダ）はホームディレクトリを表す特別な記号です。
例: `~` = `/Users/tanaka`
:::

---

### Q5. 以下のプロンプトで「tanaka」が表している情報は何ですか？

```
tanaka@MacBook Documents %
```

- A) コンピュータ名
- B) ユーザー名
- C) 現在のフォルダ名
- D) シェルの種類

:::details 答えを見る
**正解: B**

`tanaka@MacBook Documents %` の構造:
- tanaka = ユーザー名
- MacBook = コンピュータ名（ホスト名）
- Documents = 現在のフォルダ（カレントディレクトリ）
- % = プロンプト記号
:::

---

### Q6. 「今いる場所（カレントディレクトリ）」を表示するコマンドはどれですか？

- A) `ls`
- B) `cd`
- C) `pwd`
- D) `dir`

:::details 答えを見る
**正解: C**

`pwd` = **P**rint **W**orking **D**irectory（作業ディレクトリを表示）
:::

---

### Q7. フォルダの中身を一覧表示するコマンドはどれですか？

- A) `pwd`
- B) `ls`
- C) `cd`
- D) `list`

:::details 答えを見る
**正解: B**

`ls` = **L**i**s**t（一覧表示）

オプション:
- `ls -l` 詳細表示
- `ls -a` 隠しファイルも表示
- `ls -la` 両方
:::

---

### Q8. 1つ上のフォルダに移動するコマンドはどれですか？

- A) `cd ~`
- B) `cd ..`
- C) `cd -`
- D) `cd /`

:::details 答えを見る
**正解: B**

- `cd ..` = 1つ上のフォルダに移動
- `cd ~` = ホームディレクトリに移動
- `cd -` = 直前の場所に戻る
- `cd /` = ルートディレクトリに移動
:::

---

### Q9. Tab補完の説明として正しいものはどれですか？

- A) コマンドを実行する
- B) 入力途中のファイル名やフォルダ名を自動補完する
- C) ターミナルを閉じる
- D) 新しいタブを開く

:::details 答えを見る
**正解: B**

Tabキーを押すと、入力途中のファイル名やフォルダ名が自動で補完されます。

例: `cd Doc` + Tab → `cd Documents/`
:::

---

### Q10. ターミナルを終了するコマンドはどれですか？

- A) `quit`
- B) `close`
- C) `exit`
- D) `end`

:::details 答えを見る
**正解: C**

`exit` でターミナル（シェル）を終了できます。

他の方法:
- `Ctrl + D`（入力がない状態で）
- ウィンドウの×ボタン
:::

---

## 結果

### 8問以上正解の場合

**合格です！おめでとうございます！**

Step 1「黒い画面の正体を知ろう」を完了しました。
次はStep 2「ファイルとフォルダを操ろう」に進みましょう。

### 7問以下の場合

**もう少し復習しましょう**

間違えた問題の内容を、該当するセクションで復習してください：

| 問題 | 復習セクション |
|------|---------------|
| Q1-Q3 | チャプター2 ターミナル・シェル・CLIの違い |
| Q4-Q5 | チャプター4 プロンプトを読み解く |
| Q6-Q10 | チャプター5 最初の3コマンド |

---

## Step 1 完了！

お疲れさまでした！

### 学んだこと

- ターミナル、シェル、CLIの違い
- ターミナルの起動方法
- プロンプトの読み方
- 基本コマンド: `pwd`, `ls`, `cd`

### 次のステップ

**Step 2: ファイルとフォルダを操ろう**

ファイルやフォルダの作成・削除など、実際の操作を学びます。
